
const Katakana = require("../models/Katakana");
const Hiragana = require("../models/Hiragana");

const switcherType = (type) => {

    switch(type){
        case "katakana" : return Katakana; break
        case "hiragana" : return Hiragana; break
    }

}
module.exports = {switcherType}